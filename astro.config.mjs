import { defineConfig } from 'astro/config';

// User site on GitHub Pages. When the custom domain lands, change `site` only.
export default defineConfig({
  site: 'https://madbobbitt.github.io',
});
